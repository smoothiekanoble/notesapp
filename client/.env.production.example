# This is an example file for production.
# Copy this to a new file named .env.production and replace the URL with your actual Render backend URL.
# You will also need to set this environment variable in your Vercel project settings.
VITE_API_BASE_URL=https://your-render-backend.onrender.com
